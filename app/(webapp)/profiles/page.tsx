import ProfilePageClient from './page.client';

export default function ProfilePage() {
  // You can add server-side logic here if needed, like fetching initial data
  return <ProfilePageClient />;
}
