import SettingsPageClient from './page.client';

export default function SettingsPage() {
  // Fetch initial data if needed on the server
  // For now, just render the client component
  return <SettingsPageClient />;
}
